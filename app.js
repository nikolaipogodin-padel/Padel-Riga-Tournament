# Текущий статус разработки (Padel Riga Tracker)
**Версия кода:** v31 (baseline для новой ветки)
**Версия ТЗ:** v7.0 (Master)
**Последнее изменение:** 2026-04-12
**Активная ветка:** feature/premium-engine-v7

# Реализованные модули (Status Check)
- [x] **Архитектура SPA**: Маршрутизация и рендеринг через `render()` работают.
- [x] **Система состояний**: `STORE` + `persist()` обеспечивают автологин.
- [x] **Регистрация**: Базовая логика сбора данных готова (требует UI-шлифовки по ТЗ v7.0).
- [x] **Профиль**: Просмотр и редактирование данных игрока.
- [x] **UI Baseline**: Внедрены основы Dark Theme и Tailwind-конфигурация.

# Что нового в ТЗ v7.0 (Нужно внедрить)
- [ ] **Engine**: Автоматическое формирование пар при переходе в `LIVE`.
- [ ] **Engine**: Расчет `tourDuration` по формуле из раздела 5.
- [ ] **Waitlist**: Логика автоматической очереди при заполнении `maxPlayers`.
- [ ] **Validation**: "Живая" блокировка кнопки регистрации до заполнения всех полей.
- [ ] **Premium UI**: Полное соответствие эталонным экранам (стекломорфизм, неоновые акценты).

# Известные баги (BUG-BOARD)
- **BUG-001**: Связь участников в турнире идет через копирование объектов. *Решение по v7.0:* Переписать на использование только `userId`.
- **BUG-002**: Кнопка регистрации не имеет состояния `disabled` (UX-проблема).
- **BUG-003**: Отсутствует логика балансировки пар по уровню (Level).

# План на следующую итерацию (Шаг 2: UX & Engine)
1. Синхронизировать `app.js` с ТЗ v7.0 (убрать дублирование данных профилей).
2. Написать функцию `generateTournamentSchedule()` с учетом кортов и времени.
3. Обновить `style.css` для достижения "Premium Dark Look" (использовать градиенты и размытие из референсов).
